[tox]
envlist = py35,py36

[testenv]
passenv =
    TRAVIS
    TRAVIS_*
    APPVEYOR
    APPVEYOR_*
deps =
    -r{toxinidir}/requirements.txt
    -r{toxinidir}/dev-requirements.txt
    coveralls
commands =
    pytest --cov=mesdossiers -n 2 {posargs}
    coveralls

[flake8]
max-complexity = 10
